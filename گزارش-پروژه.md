# Web LLM Bridge - گزارش کامل پروژه

## خلاصه

**نام:** Web LLM Bridge
**نوع:** Virtual LLM API Gateway با Model Compatibility Proxy (MCP)
**زبان:** Python 3.13
**فریم‌ورک:** Flask + Playwright
**هدف:** مسیریابی درخواست‌های HTTP محلی به ChatGPT Web از طریق Chrome DevTools Protocol

---

## معماری

```
Agent Runtime
      |
Virtual LLM API (سازگار OpenAI)
      |
      v
+---------------------+
|      لایه MCP       |  core/mcp.py
+---------------------+
      |
      v
+---------------------+
|  رجیستری پروایدر   |  core/provider_registry.py
+---------------------+
      |
      v
پروایدر ChatGPT Web (آداپترهای DOM/Network)
```

---

## ساختار پروژه

```
D:\Code\mcp-web-bridge\
├── main.py                      # نقطه ورود Flask
├── config.yaml                  # پیکربندی
├── requirements.txt
├── README.md
├── manage.py                    # ابزار CLI
├── service_manager.ps1          # مدیر سرویس ویندوز
├── control_panel.py             # داشبورد وب
├── adapters/
│   └── chatgpt_web_provider.py  # پروایدر ChatGPT Web (async)
├── core/
│   ├── providers.py             # کلاس پایه Provider
│   ├── mcp.py                   # لایه MCP
│   ├── provider_registry.py
│   ├── governance.py            # احراز هویت، rate limit
│   ├── config.py
│   └── files.py
├── docs/architecture/           # مستندات معماری
├── tests/                       # تست‌ها
└── logs/                        # لاگ‌ها
```

---

## چه چیزهایی کار می‌کنند ✅

| کامپوننت | وضعیت |
|-----------|--------|
| اینترفیس Provider | ✅ ۳۱ تست واحد پاس می‌شود |
| لایه MCP | ✅ تست‌ها پاس |
| رجیستری پروایدر | ✅ تست‌ها پاس |
| Governance | ✅ تست‌ها پاس |
| بارگذاری Config | ✅ کار می‌کند |
| Flask App | ✅ روی پورت ۵۰۰۰ بالا می‌آید |
| Swagger Docs | ✅ در `/docs/` |
| کنترل پنل | ✅ در `/panel/` (نمودار اصلاح شد) |
| استفاده مستقیم از Provider | ✅ تست شد - پاسخ `DIRECT_TEST_OK` |
| ۳۱ تست واحد | ✅ همه پاس |

---

## مشکلات شناخته شده

### مشکل ۱: Playwright Async API در Flask (بحرانی)

**علائم:**
- اولین درخواست چت کار می‌کند
- درخواست‌های بعدی با خطا شکست می‌خورند: `Page.wait_for_selector: 'NoneType' object has no attribute 'send'`

**علت اصلی:**
Playwright async API در context درخواست Flask ناپایدار است. اتصال WebSocket به CDP بعد از اولین عملیات قطع می‌شود.

**راه‌حل موقت:**
استفاده مستقیم از Provider در اسکریپت Python.

**راه‌حل دائمی:**
Refactor به یکی از:
1. معماری Queue-based
2. Subprocess برای هر درخواست
3. اتوماسیون HTTP-based

---

### مشکل ۲: تداخل ماژول‌های Legacy (حل شده)

ماژول‌های قدیمی با پسوند `.bak` بک‌آپ گرفته شدند.

---

### مشکل ۳: سرویس ویندوز و Chrome (طراحی)

سرویس ویندوز در session 0 اجرا می‌شود و به Chrome کاربر دسترسی ندارد.

---

## دستورالعمل تست

### پیش‌نیازها
1. Python 3.10+ با venv در `.venv/`
2. Chrome نصب شده
3. لاگین فعال در ChatGPT

### راه‌اندازی

```powershell
# 1. Chrome با دیباگ
Start-Process "C:\Program Files\Google\Chrome\Application\chrome.exe" -ArgumentList "--remote-debugging-port=9222", "--user-data-dir=C:\chrome-debug"
# → در Chrome به chatgpt.com بروید و لاگین کنید

# 2. سرور
cd D:\Code\mcp-web-bridge
.venv\Scripts\python.exe main.py
```

### تست ۱: تست‌های واحد (همیشه کار می‌کند)
```bash
.venv\Scripts\python.exe -m pytest tests/test_core.py -v
```

### تست ۲: Health Endpoints
```bash
curl http://localhost:5000/health
curl http://localhost:5000/modes
```

### تست ۳: استفاده مستقیم (کار می‌کند)
```powershell
.venv\Scripts\python.exe -c "
import asyncio
from adapters.chatgpt_web_provider import create_chatgpt_web_provider
from core.providers import ChatCompletionRequest

async def test():
    provider = create_chatgpt_web_provider()
    request = ChatCompletionRequest(
        model='chatgpt-web',
        messages=[{'role': 'user', 'content': 'Reply only with TEST_OK'}]
    )
    response = await provider.chat_completion(request)
    print(response.choices[0].message.content)
    await provider.close()

asyncio.run(test())
"
# خروجی مورد انتظار: TEST_OK
```

### تست ۴: Flask API (فقط اولین درخواست)
```bash
# کار می‌کند:
curl -X POST http://localhost:5000/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{"messages":[{"role":"user","content":"Say hi"}]}'

# دومین درخواست باگ دارد - نیاز به restart سرور
```

### تست ۵: کنترل پنل
مرورگر: `http://localhost:5000/panel/`

### تست ۶: Swagger Docs
مرورگر: `http://localhost:5000/docs/`

---

## نتیجه‌گیری

| جنبه | وضعیت |
|------|--------|
| طراحی معماری | ✅ کامل و مستند شده |
| پیاده‌سازی هسته | ✅ تست‌ها پاس |
| استفاده مستقیم | ✅ کاملاً کار می‌کند |
| Flask API | ⚠️ فقط اولین درخواست |
| کنترل پنل | ✅ کار می‌کند |
| مستندات | ✅ جامع |
| آماده production | ❌ نیاز به رفع مشکل Playwright |

**خلاصه:** معماری solid است و هسته کار می‌کند. یکپارچه‌سازی Flask مشکل Playwright دارد. فعلاً از اسکریپت‌های مستقیم Python استفاده کنید.